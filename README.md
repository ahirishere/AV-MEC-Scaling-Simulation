The code implements a stylized two-link morning peak model with:
- BPR delay functions
- Logit route choice for AVs and humans
- A dynamic tolling controller that sets Pigouvian (MEC-based) tolls
- Two controller variants: naïve (unscaled MEC) and scaled (MEC / p_eff)
